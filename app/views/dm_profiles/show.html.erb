<div class="profile-wrapper">
  <div class="profile-summary">
    <p id="notice"><%= notice %></p>
    <%= image_tag @user.avatar.url(:medium) %>
    <div class="profile-text">
      <p>
        <strong>Username:</strong>
        <%= @user.username %>
      </p>

      <p>
        <strong>Age:</strong>
        <%= @user.age %>
      </p>

      <p>
        <strong>Experience:</strong>
        <%= translate_experience_level(@dm_profile.experience_level) %>
      </p>

      <p>
        <strong>Likes to play:</strong>
        <%= get_ruleset_strings(@dm_profile).join(", ") %>
      </p>

      <p>
        <strong><%= translate_online_play(@dm_profile) %></strong>
      </p>


    <!--  <% unless logged_in? && (current_user.id == params[:user_id])  %>
        <% #if within_distance(User.find(@dm_profile.user_id), current_user) %>
          <p>
            <strong>Distance:</strong>
            <%= find_distance(User.find(@dm_profile.id), current_user) %>
          </p>
        <% else %>
          <p>
            <strong>Too far away!</strong>
          </p>
        <%#end %>
      <% end %>-->
    </div>
    <div class="button-container">
      <button class="contact-me-button">Contact me!</button>
    </div>
  </div>
  <div class="profile-bio">
    <p>
      <strong>Bio:</strong>
      <%= @dm_profile.bio %>
    </p>
  </div>
  <div class="more-users-box">
    <p>
      <strong>You may also like:</strong>
    </p>
  </div>
  <div class="ending-buttons">
    <%= link_to 'Edit', edit_user_dm_profiles_path(@user, @dm_profile) %> |
    <%= link_to 'Back', user_path(@user) %>
  </div>
</div>
