<p id="notice"><%= notice %></p>
<div class="profile-wrapper">
  <div class="profile-summary">
    <%= image_tag @user.avatar.url(:medium) %>
    <div class="profile-text">
      <p>
        <strong>Username:</strong>
        <%= User.find(@player_profile.user_id).username %>
      </p>

      <p>
        <strong>Age:</strong>
        <%= User.find(@player_profile.user_id).age %>
      </p>

      <p>
        <strong>Experience:</strong>
        <%= translate_experience_level(@player_profile.experience_level) %>
      </p>

      <p>
        <strong>Likes to play:</strong>
        <%= get_ruleset_strings(@player_profile).join(", ") %>
      </p>

      <p>
        <strong><%= translate_online_play(@player_profile) %></strong>
      </p>


    <!--  <% unless logged_in? && (current_user.id == params[:user_id])  %>
        <% #if within_distance(User.find(@player_profile.user_id), current_user) %>
          <p>
            <strong>Distance:</strong>
            <%= find_distance(User.find(@player_profile.id), current_user) %>
          </p>
        <% else %>
          <p>
            <strong>Too far away!</strong>
          </p>
        <%#end %>
      <% end %>-->
    </div>
    <div class="button-container">
      <button class="contact-me-button">Contact me!</button>
    </div>
  </div>
  <div class="profile-bio">
    <p>
      <strong>Bio:</strong>
      <%= @player_profile.bio %>
    </p>
  </div>
  <div class="more-users-box">
    <p>
      <strong>You may also like:</strong>
    </p>
  </div>
  <div class="ending-buttons">
    <%= link_to 'Edit', edit_user_player_profiles_path(@user, @player_profile) %> |
    <%= link_to 'Back', user_path(@user) %>
  </div>
</div>
